#include "main.h"
#include <stdio.h>
/**
 * _puts - putchar caractere
 * @s: i putchar
*/
void print_rev(char *s);
{
	char*s = len

	for ( i = 0, i <  len, i++)
	{

		_putchar(*s[i]);

	}

	for (i = len-1; i > 0; i--)
	{
		_putchar(s[i];
	}

	_putchar('\n');

}
