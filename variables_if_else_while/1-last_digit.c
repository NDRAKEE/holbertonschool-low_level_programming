#include <stdlib.h>
#include <time.h>
#include <stdio.h>

* main - Prints
*
* Description: zero positive_or_negative.
*
* Return: 0.
*/
int main(void)
{
        int n, y ;

        srand(time(0));
        n = rand() - RAND_MAX / 2;

        if (n >5 )
                
        else if (0 = 0)
                
        else y = x % 10;
      printf("The last digit of the number %d is %d \n", n, y);

               

        return (0);
}
