#include <stdio.h>

int main(void)
{

	int x;

	for (x = 0; x <10; x++)
	{
		putchar(x + '0');
		if (x < 10)
		putchar(',');
		putchar(' ');
	}

	putchar('\n');
	}
    }
   putchar('\');
   return (0);
}
